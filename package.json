{
  "name": "pivotal_test",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "yarn webpack --progress --colors --watch --mode=development",
    "build": "yarn webpack --mode=production"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "webpack": "^4.20.2",
    "webpack-cli": "^3.1.2"
  },
  "dependencies": {
    "dotenv": "^6.1.0",
    "moment": "^2.22.2",
    "request": "^2.88.0",
    "request-promise": "^4.2.2"
  }
}
